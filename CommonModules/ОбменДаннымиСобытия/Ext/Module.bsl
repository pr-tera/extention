
&Перед("ДополнитьПолучателей")
Процедура пр_ДополнитьПолучателей(Объект, Узлы)
	
	УзелДляУдаления = ПланыОбмена.ОбменУправлениеНебольшойФирмойБухгалтерия20.НайтиПоКоду("УП");
	
	Если НЕ УзелДляУдаления.Пустая() Тогда
		
		ИндексЭлемента = Узлы.Найти(УзелДляУдаления);
		
		Если ИндексЭлемента <> Неопределено Тогда
			Узлы.Удалить(ИндексЭлемента);	
		КонецЕсли; 
		
	КонецЕсли; 
	
КонецПроцедуры
